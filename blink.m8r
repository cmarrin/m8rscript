/*
    This is a test program
*/

var b = [ 1, 2, 3 ];
var c = 12.5;
var a = { b : 5+c, "foo" : b % c, 12.3 : "Bar", 54325667 : c };

print("Array b[1] = " + b[1] + "\n");
print("Object a[54325667] = " + a[54325667] + "\n");
print("Object a.b = " + a.b + "\n");

GPIO.pinMode(0x2, GPIO.OUTPUT);
Serial.begin(9600);
Serial.println("Started Blink");
var count = 0;
while (count < 1000) {
    blink();
    count++;
}

function blink()
{
    GPIO.digitalWrite(0x0002, GPIO.HIGH);
    //Serial.println("Blink on");
    print("Blink on\n");
    System.delay(200);
    GPIO.digitalWrite(2, GPIO.LOW);
    //Serial.println("Blink off");
    print("Blink off\n");
    System.delay(200); // wait a bit
}

function add(x, y, z)
{
	return function(y)
	{
		return x+y;
	};
}

//add(1, 2, 3);

var a = b * c + d * e;

// add2 = add(2);
// print(add2(5));
// 
// function multi(x)
// {
//  return { x + 3, x * 4, x / 5 };
// }
// 
// a = multi(4);
// Serial.println("a=" + a);
 
