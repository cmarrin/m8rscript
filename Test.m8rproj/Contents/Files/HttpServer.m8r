//
// Http Server
//

printf("TCPSocket Proto = '%s'\n", TCPSocket);
var sock = new TCPSocket();
printf("TCPSocket = '%s'\n", sock);


/*
function HttpServer(port)
{
	var _port = port;
	var _tcp = TCP(port);
	_tcp.connect(connection)
	_tcp.receivedData = function(data)
	{
	}
}
*/
